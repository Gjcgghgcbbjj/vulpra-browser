diff --git a/dom/media/webrtc/MediaEngineRemoteVideoSource.cpp b/dom/media/webrtc/MediaEngineRemoteVideoSource.cpp
index 1b7c214abee9736dd6f153cbb56b540f0220f9d1..9b312d08dd52369f5838dae134123a54d14e3a98 100644
--- a/dom/media/webrtc/MediaEngineRemoteVideoSource.cpp
+++ b/dom/media/webrtc/MediaEngineRemoteVideoSource.cpp
@@ -192,12 +192,13 @@ MediaEngineRemoteVideoSource::MediaEngineRemoteVideoSource(
       mDeviceUUID(NS_ConvertUTF16toUTF8(aMediaDevice->mRawID)) {
   LOG("%s", __PRETTY_FUNCTION__);
   if (mCapEngine == camera::CameraEngine) {
-    // Only cameras can have a facing mode.
-    Maybe<VideoFacingModeEnum> facingMode =
-        GetFacingMode(mMediaDevice->mRawName);
+    Maybe<VideoFacingModeEnum> facingMode = mMediaDevice->mFacingMode;
+    if (facingMode.isNothing()) {
+      facingMode = GetFacingMode(mMediaDevice->mRawName);
+    }
     if (facingMode.isSome()) {
-      mFacingMode.emplace(
-          NS_ConvertASCIItoUTF16(dom::GetEnumString(*facingMode)));
+      mFacingMode.emplace(NS_ConvertASCIItoUTF16(
+          dom::GetEnumString(*facingMode)));
     }
   }
 }
